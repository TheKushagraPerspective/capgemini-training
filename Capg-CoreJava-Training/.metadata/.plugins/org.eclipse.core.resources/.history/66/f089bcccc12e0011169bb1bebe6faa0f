package com.capg.set;

import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.TreeSet;

public class SetDemo {
	public static void main(String[] arg) {
//		 System.out.println("----- HASHSET DEMO -----");

//        HashSet<Integer> hs = new HashSet<>();
//        
//        hs.add(10);
//        hs.add(30);
//        hs.add(20);
//        hs.add(30);   // ignored
//        System.out.println(hs);
//        
//        hs.add(null);
//        System.out.println(hs);
//        
//     // remove()
//        hs.remove(20);
//        System.out.println("After remove: " + hs);
//
//        // contains()
//        System.out.println("Contains 10? " + hs.contains(10));
//
//        // size()
//        System.out.println("Size: " + hs.size());
//
//        // isEmpty()
//        System.out.println("Is Empty? " + hs.isEmpty());
//        
//     // iteration (for-each)
//        System.out.println("For-each:");
//        for(Integer i : hs){
//            System.out.print(i + " ");
//        }
//        System.out.println();
//
//        // iterator
//        System.out.println("Iterator:");
//        Iterator<Integer> it = hs.iterator();
//        while(it.hasNext()){
//            System.out.print(it.next() + " ");
//        }
//        System.out.println();
//
//        // clear()
//        hs.clear();
//        System.out.println("After clear: " + hs);
		 
		// ================= LINKEDHASHSET =================
//		 System.out.println("\n----- LINKEDHASHSET DEMO -----");
//
//        LinkedHashSet<Integer> lhs = new LinkedHashSet<>();
//        lhs.add(10);
//        lhs.add(30);
//        lhs.add(20);
//        lhs.add(30);
//        lhs.add(null);
//        System.out.println(lhs);
		 
		 
		// ================= TREESET =================
        System.out.println("\n----- TREESET DEMO -----");

        TreeSet<Integer> ts = new TreeSet<>();

        ts.add(30);
        ts.add(10);
        ts.add(20);
        ts.add(20);
//        ts.add(null);

        System.out.println("TreeSet (Ascending): " + ts);
        
     // first() & last()
        System.out.println("First: " + ts.first());
        System.out.println("Last: " + ts.last());

        // pollFirst() & pollLast()
        System.out.println("Poll First: " + ts.pollFirst());
        System.out.println("Poll Last: " + ts.pollLast());

        System.out.println("After polling: " + ts);
        
        ts.add(10);
        ts.add(30);
        
        TreeSet<Integer> des = new TreeSet<>(Collections.reverseOrder());
        des.addAll(ts);
        System.out.println(des);
        
        
     // ================= SORTING HASHSET =================
        System.out.println("\n----- SORT HASHSET -----");

        HashSet<Integer> set = new HashSet<>();
        set.add(25);
        set.add(5);
        set.add(15);

        // Convert to list
        ArrayList<Integer> list = new ArrayList<>(set);

        // Ascending
        Collections.sort(list);
        System.out.println("Ascending: " + list);

        // Descending
        Collections.sort(list, Collections.reverseOrder());
        System.out.println("Descending: " + list);


        // ================= EDGE CASES =================

        // TreeSet does NOT allow null
        try {
            TreeSet<Integer> ts2 = new TreeSet<>();
            ts2.add(null); // ❌ Exception
        } catch (Exception e) {
            System.out.println("TreeSet null error: " + e);
        }

        // HashSet allows one null
        HashSet<Integer> hs2 = new HashSet<>();
        hs2.add(null);
        hs2.add(null); // ignored
        System.out.println("HashSet null: " + hs2);
        
	}
}
